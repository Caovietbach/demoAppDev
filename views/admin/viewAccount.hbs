<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Acount</title>
</head>
<body>
    <h1>FPT University</h1>
    <h2>List Account</h2>
    <div class = "bar">
        <a href="/admin/home">Menu</a>
        <a href="/admin/viewIdea">View Idea</a>
        <a href="/admin/viewAccount">View Account</a>
        <a href="/logout">Logout</a> 
    </div>
    <div class="container">
    <table class="table table-dark table-striped">
            <thead>
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Account</th>
                    <th scope="col">Role</th>
                    <th scope="col">Option</th>
                </tr>
            </thead>
            {{#each accounts}}
            <tbody>
                <tr>
                    <td>{{_id}}</td>
                    <td>{{userName}}</td>
                    <td>{{role}}</td>
                    <td>
                        <button type="submit" class="btn btn-danger">
                            <a href="/admin/deleteAccount?id={{_id}}" onclick="return confirm('Are you sure?')" style="color:red">
                                Delete
                            </a> </button>
                        <br><br>
                        <button type="submit" class="btn btn-default">
                            <a href="/admin/updateAccount?id={{_id}}">
                                Update
                            </a> </button>
                    </td>
                </tr>
            </tbody>
            {{/each}}
        </table>
</div>
</body>
</html>